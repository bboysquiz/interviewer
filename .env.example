# Frontend API URL for local development.
# Leave empty in production when frontend and backend are served from one domain.
VITE_API_BASE_URL=http://localhost:3000

# Public domain for reverse proxy / HTTPS deployment.
APP_DOMAIN=your-subdomain.duckdns.org
